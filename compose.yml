services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - path: ./backend/.env
        required: false
    volumes:
      - ./backend:/opt/app
    restart: unless-stopped
    container_name: backend

# vim: ft=yaml.docker-compose
